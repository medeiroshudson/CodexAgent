# /migrate-context

Use `$context-curation` to discover a navigation-based Markdown context tree, run `codex-agent migrate navigation` in preview mode, explain migrated and skipped files, and apply only after explicit approval.
