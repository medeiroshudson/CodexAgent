# /doctor

Inspect the plugin installation, repository marketplace, context index, Node.js runtime, project agent templates, and hook trust status. Prefer the `codex-agent doctor --json` CLI when available. Do not mutate configuration during diagnosis.

