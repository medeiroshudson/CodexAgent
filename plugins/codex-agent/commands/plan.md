# /plan

Use `$plan-and-approve` to inspect repository context and propose the smallest implementation approach. If the user already approved a concrete plan, summarize that authority and continue without requesting duplicate approval.

