#!/usr/bin/env node

process.stdout.write(
  "Before declaring completion, inspect the final diff and report fresh validation evidence in direct prose per $humanizer: state the outcome first, keep statuses, commands, severities, and gaps exact, and remove staging, decoration, inflated significance, and closing offers. State the authority basis, since a material change without an approved plan is a gap rather than a success. Optional $context-harvest or $context-curation work stays separate from task completion and never promotes knowledge without explicit approval.\n"
);
