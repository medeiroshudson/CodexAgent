#!/usr/bin/env node

process.stdout.write(
  "Before declaring completion, inspect the final diff and report fresh test, lint, typecheck, build, packaging, or runtime evidence proportional to the change.\n"
);

