interface:
  display_name: "Agent Orchestration"
  short_description: "Route Codex subagents by capability and integrate evidence"
  default_prompt: "Use $agent-orchestration to coordinate the independent workstreams in this repository task with quality-first, capability-appropriate subagent models, explicit ownership, integrated verification, and ephemeral state unless I explicitly request resumability."
