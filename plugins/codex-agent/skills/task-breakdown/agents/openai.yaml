interface:
  display_name: "Task Breakdown"
  short_description: "Build dependency-safe executable task graphs"
  default_prompt: "Use $task-breakdown to turn this approved multi-component change into atomic task packets with contracts, dependencies, safe concurrency, and validation."
