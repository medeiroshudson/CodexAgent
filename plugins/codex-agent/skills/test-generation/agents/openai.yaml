interface:
  display_name: "Test Generation"
  short_description: "Prove behavior with focused deterministic tests"
  default_prompt: "Use $test-generation to cover this behavior or regression at the correct boundary with the smallest deterministic test set."
