interface:
  display_name: "Context Curation"
  short_description: "Curate durable indexed project knowledge"
  default_prompt: "Use $context-curation to classify, deduplicate, preview, and only then save or migrate this durable project knowledge."
