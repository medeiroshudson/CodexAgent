interface:
  display_name: "Verify Completion"
  short_description: "Map acceptance criteria to fresh evidence"
  default_prompt: "Use $verification-before-completion to inspect the integrated state and map every acceptance criterion to fresh proportional evidence before completion."
