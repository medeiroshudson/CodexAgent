interface:
  display_name: "External Research"
  short_description: "Verify versioned external contracts with sources"
  default_prompt: "Use $external-research to resolve this implementation-relevant external contract with version-matched authoritative citations and explicit uncertainty."
