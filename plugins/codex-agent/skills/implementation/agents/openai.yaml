interface:
  display_name: "Implementation"
  short_description: "Implement authorized changes with fresh evidence"
  default_prompt: "Use $implementation to deliver this authorized bounded change, preserve unrelated work, validate incrementally, and report fresh evidence."
