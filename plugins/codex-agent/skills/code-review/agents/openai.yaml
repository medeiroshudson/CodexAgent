interface:
  display_name: "Code Review"
  short_description: "Find evidence-backed risks without editing"
  default_prompt: "Use $code-review to review the complete relevant change against its intended behavior and return only evidence-backed findings without editing files."
