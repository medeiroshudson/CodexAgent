interface:
  display_name: "Context Init"
  short_description: "Bootstrap evidence-backed project context"
  default_prompt: "Use $context-init to show the complete first-context approval plan; after I approve that displayed plan, apply it using the retained integrity ID."
